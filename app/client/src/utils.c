/* =============================================================================
 * utils.c — Shared utility functions for the GUI client
 *
 * Reserved for future utility functions.  Currently empty because all
 * utilities live in the module that owns them.
 * ============================================================================= */

#include "../include/app_state.h"

/* placeholder — add helpers here as the client grows */
